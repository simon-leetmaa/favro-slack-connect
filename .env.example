# Favro-Slack Integration Configuration
# Copy this file to .env and fill in your values

# Required Settings
# ----------------
# Your Slack Bot Token (starts with xoxb-)
# This token should have the following permissions:
# - users:read (to read user information)
# - users:read.email (to match users by email)
# - chat:write (to send messages)
# - im:write (to open DM channels)
SLACK_BOT_TOKEN=xoxb-your-token-here

# Your Favro webhook secret (generated when creating the webhook in Favro)
FAVRO_WEBHOOK_SECRET=your-secret-here

# The public URL where your webhook will be accessible
# Example: https://your-server.com/webhook or http://localhost:3000/webhook for testing
WEBHOOK_URL=http://localhost:3000/webhook

# Optional Settings
# ----------------
# Port for the server to listen on (defaults to 3000)
PORT=3000

# Log level (debug, info, warn, error)
LOG_LEVEL=info
